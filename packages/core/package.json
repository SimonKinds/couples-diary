{
  "name": "couples-diary-core",
  "version": "0.1.0",
  "main": "dist/index.js",
  "author": "Simon Kindström",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "babel index.js src/* --out-dir dist",
    "gen-flow-files": "find index.js src -type f -exec flow gen-flow-files --out-dir dist {} \\;",
    "prepublish": "npm run build && npm run gen-flow-files",
    "test": "jest src"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.73.0",
    "jest": "^23.1.0"
  }
}
